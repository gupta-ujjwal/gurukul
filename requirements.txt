# Core dependencies
flask==2.3.3
flask-socketio==5.3.6
python-dotenv==1.0.0
eventlet==0.33.3  # For WebSocket support
gevent==23.9.1    # Alternative WebSocket support
gevent-websocket==0.10.1  # WebSocket for gevent
pypdf==3.15.1     # For PDF extraction

# Async support
aiohttp==3.8.5
asyncio==3.4.3
websockets==11.0.3  # For WebSocket testing script

# Logging and debugging
colorlog==6.7.0

# JAF dependencies (assuming these are already installed in the environment)
# If not, uncomment the following lines:
# jaf
# litellm
